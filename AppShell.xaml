<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MAU.Notes.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Views="clr-namespace:MAU.Notes.Views"
    Shell.FlyoutBehavior="Disabled">
    <TabBar>
        <ShellContent
        Title="Notes"
        ContentTemplate="{DataTemplate views:AllNotesPages}"
        Icon="icon_notes" />

    <ShellContent
        Title="About"
        ContentTemplate="{DataTemplate views:AboutPage}"
        Icon="icon_about" />
    </TabBar>
    
</Shell>
